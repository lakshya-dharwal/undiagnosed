# Obvious repo policy — lakshya-dharwal/undiagnosed
# Auto-detected from GitHub repo settings on 2026-09-17.
branches:
  default: main
merge:
  method: merge        # GitHub default; merge, squash, and rebase are all enabled
  requireHumanMerge: true
