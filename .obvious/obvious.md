# obvious.md — lakshya-dharwal/undiagnosed

## Status: empty repository

This repository is currently **empty of application code**. It contains only
project context documentation (`README.md`, `docs/`), a Supabase CLI config
template (`supabase/config.toml`) with an empty `supabase/migrations/`
directory, and environment templates (`.env.example`, `.gitignore`).

There are no dependency manifests, no Dockerfile/Compose/Makefile, and no
source files — so there is no application to run, no commands to document,
and no dev stack to bring up yet.

## What comes next

The stack, commands, ports, env vars, and codebase map will be filled in by
the next onboarding/setup run once application code lands in this repo.

Nothing in this file documents how to run the app, because there is no app
to run yet. Do not infer or scaffold a stack from the docs alone.
