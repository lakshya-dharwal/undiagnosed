# OpenAI API key, used for text-embedding-3-small embeddings.
# Not covered by Obvious/Autobuild platform credits — a separate key is required.
OPENAI_API_KEY=

# Supabase project connection (pgvector extension enabled).
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
